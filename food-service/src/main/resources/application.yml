spring:
  application:
    name: "food-service"
  profiles:
    active: prod
  config:
    import: "configserver:http://localhost:9096"
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest